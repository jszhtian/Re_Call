//============================================================================
//
// イベント絵定義
//
//============================================================================
#=ES.EV.DEFINE


	//---------------------------------------------------------------------
	// イベント絵のデフォルト切替時間(ミリ秒)
	//   \EV() マクロを使用した場合のデフォルト時間です
	//---------------------------------------------------------------------
		@T(24,01)=800

	//---------------------------------------------------------------------
	// イベント絵簡易利用文字列定義
	//---------------------------------------------------------------------

		//例：「出会い１」という文字列を定義する
		//
		// ここで「出会い１」という文字列で設定を定義しておくと、
		// スクリプト中に \EV("出会い１") という記述があった場合、
		// 以下に設定したパラメータが自動で設定された状態で読み込まれます。
		//

	//	\EV.FILE("ev001_1")   // ファイル名
	//	\EV.FADE(1500)        // フェード時間
	//	\EV.TR("5")           // トランジション文字列
	//	\EVDEF("出会い１")    // 登録.



RETURN[]
